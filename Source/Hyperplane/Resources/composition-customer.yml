#@ load("funcs.star", "dolittle_patch_sets_metadata_from_spec", "dolittle_references")

apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: customer
  labels:
    crossplane.io/xrd: xcustomers.dolittle.io
spec:
  #! test: #@ dolittle_references("v1", "Namespace", "spec.metadata.customer-id")
  compositeTypeRef:
    apiVersion: dolittle.io/v1alpha1
    kind: XCustomer
  patchSets: #@ dolittle_patch_sets_metadata_from_spec("spec.metadata.")
  resources:
  - name: namespace
    base:
      apiVersion: kubernetes.crossplane.io/v1alpha1
      kind: Object
      spec:
        providerConfigRef:
          name: provider-kubernetes
        forProvider:
          manifest:
            apiVersion: v1
            kind: Namespace
    patches:
    - type: PatchSet
      patchSetName: dolittle-metadata
    - fromFieldPath: metadata.annotations[dolittle.io/customer-id]
      toFieldPath: metadata.name
      transforms:
      - type: string
        string:
          fmt: "customer-%s"